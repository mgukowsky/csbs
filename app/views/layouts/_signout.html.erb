<a href="<%= edit_user_url(current_user.id) %>">Change account information</a>
<form class="sign-out-button group" action="<%= session_url %>" method="post">
  <%= auth_token %>
  <input type="hidden" name="_method" value="delete">
  <button>Sign Out</button>
</form>
